import java.time.*
import java.time.format.DateTimeFormatter

pipeline {
    agent any

    environment {
        REPOSITORY_URI = "backend-security"
    }

    stages {

        stage('Construyendo Tag') {
            steps {
                script {
                    // Fecha actual en formato yyMMdd.HHmm UTC
                    def now = new Date()
                    def dateNow = now.format("yyMMdd.HHmm", TimeZone.getTimeZone('UTC'))

                    // Construcción del tag con número de build
                    def dockerTag = "${dateNow}.${BUILD_NUMBER}"
                    def repositoryUriApp = "${REPOSITORY_URI}:${dockerTag}"

                    // Guardar en variables del pipeline
                    env.DATE_NOW = dateNow
                    env.DOCKER_TAG = dockerTag
                    env.REPOSITORY_URI_APP = repositoryUriApp

                    echo "🕒 Fecha actual: ${env.DATE_NOW}"
                    echo "🏷️ Tag generado: ${env.REPOSITORY_URI_APP}"
                }
            }
        }

        stage('Desplegando con Docker Compose') {
            steps {
                script {
                    echo "🚀 Iniciando despliegue con Docker Compose..."
                    
                    // Asegúrate de que docker-compose.yml esté en ./devops/
                    sh """
                        echo '📦 Usando imagen: ${REPOSITORY_URI_APP}'
                        docker compose -f ./devops/docker-compose.yml up -d --build
                        docker image prune -f
                    """
                }
            }
        }
    }

    post {
        success {
            echo "🎉 Pipeline completado exitosamente: ${env.REPOSITORY_URI_APP}"
            sh "docker ps"
        }
        failure {
            echo "❌ Error durante la ejecución del pipeline. Verifica los logs en Jenkins."
        }
    }
}
